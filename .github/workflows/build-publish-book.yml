name: Build Publish Book
on:
  push:
    branches:
      - main
		
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@main
      - name: pppp
        run: |
          echo 'aaa'
          echo 'bbb'

