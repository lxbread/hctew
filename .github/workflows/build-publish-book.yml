name: Build Publish Book
on:
  push
		
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@main
      - name: pppp
        run: |
          echo 'aaa'
          echo 'bbb'

