name: Build Publish Book
on:
  push:
    branches:
      - main
jobs:
  build-book-1-step:
    name: Build Book
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@main
  
		
